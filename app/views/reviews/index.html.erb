<h1>Review#index</h1>

<% @reviews.each do |review|%>
<span>タイトル︰</span>
  <%= review.title %>
  <br>
  <span>レビュー︰</span>
  <%= link_to shop_review_path(@shop.id, review.id) do%>
    <%= review.comment.truncate(100) %>
  <% end %>
  <hr>
<% end %> 
<%= paginate @reviews %>

<%= link_to '戻る', :back %>

