<%= render 'layouts/flash'%>
<p>マイページ</p>
<ul>
  <li>名前:<%= @user.name %></li>
  <li>年齢:<%= @user.age %>歳</li>
  <li>性別:<%= @user.sex %></li>
  <% if  @user.fav_brand.nil? %>
    <li>お気に入りブランド:なし</li>
  <% else %>
    <li>お気に入りブランド:<%= @user.fav_brand %></li>
  <% end %>
</ul>

<%= link_to 'プロフィールを編集', edit_user_path(current_user.id) %>
<%= link_to 'お問い合わせ', '/pages/contact'%>

<p><span><%=current_user.name %> :投稿ショップ一覧</span></p>


<% @shops.each do |shop|%>
  
  <%= link_to shop.name , shop_path(shop.id) %>
  <hr>
<% end %>