
<p>マイページ</p>
<ul>
  <li>名前:<%= @user.name %></li>
  <li>年齢:<%= @user.age %>歳</li>
  <li>性別:<%= @user.sex %></li>
  <% if  @user.fav_brand.nil? %>
    <li>お気に入りブランド:なし</li>
  <% else %>
    <li>お気に入りブランド:<%= @user.fav_brand %></li>
  <% end %>
</ul>

<%= link_to 'プロフィールを編集', edit_user_path(current_user.id) %>